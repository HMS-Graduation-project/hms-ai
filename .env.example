# ──────────────────────────────────────
# HMS AI Service - Local Development
# Copy to .env: cp .env.example .env
# When running via docker-compose, env is injected from hms-infra/.env
# ──────────────────────────────────────

# Server
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development
